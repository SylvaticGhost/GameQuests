import { styled } from "@mui/material/styles";
import {Box} from '@mui/material';

const BoxForPrfl = styled(Box)`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 3.5;
`;

export default BoxForPrfl;
